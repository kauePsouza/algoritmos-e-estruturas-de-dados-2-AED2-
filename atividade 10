#include <stdio.h>

int main ()
{
 int num1, num2, num3, numM, p;
 
printf("Digite 3 numeros inteiros: ");

scanf("%d", &num1);
numM = num1;
p = 1;

scanf("%d", &num2);
if(num2 > num1){
numM = num2;
p = 2;
}
scanf("%d", &num3);
if(num3 > numM)
p = 3;

printf("Posicao = %d", p);
}
